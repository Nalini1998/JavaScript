
// Create a variable named Kelvin, and set it equal to 293

//const kelvin = 293;
const kelvin = 0;
// Create a variable named Celsius, and set it is 273 degrees less than Kelvin
const Celsius = kelvin - 273;

// Use the fahrenheit equation to calculate Fahrenheit
// Use the .floor() method to round down the Fahrenheit temperature
Fahrenheit = Math.floor(Celsius * (9/5) + 32);

// Convert celsius to the Newton scale
Newton = Math.floor(Celsius * (33/100));

console.log(`The temperature is ${Fahrenheit} degrees Fahrenheit.`);
console.log(`The temperature is ${Newton} degrees Fahrenheit.`);
